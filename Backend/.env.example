# MongoDB Atlas Connection String
# URI de conexión a MongoDB Atlas (nube)
# Formato: mongodb+srv://usuario:contraseña@cluster.mongodb.net/nombre_db?options
MONGO_URI=mongodb+srv://usuario:contraseña@cluster0.kim4hv5.mongodb.net/nom035DB?retryWrites=true&w=majority&appName=Cluster0

# MongoDB Local (alternativa)
# MONGO_URI=mongodb://localhost:27017/nom035DB

# Configuración del Backend
NODE_ENV=production
PORT=3000

# Instrucciones:
# 1. Copia este archivo como .env
# 2. Reemplaza 'usuario' y 'contraseña' con tus credenciales de MongoDB Atlas
# 3. Asegúrate de que el .env esté en .gitignore
